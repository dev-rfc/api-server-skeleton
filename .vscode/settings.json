{
    "editor.tabSize": 2,
    "phpunit.command": "docker-compose run --rm phpfpm",
    "phpunit.phpunit": "/app/vendor/bin/phpunit",
    "phpunit.paths": {
        "${workspaceFolder}/": "/app/",
    },
}